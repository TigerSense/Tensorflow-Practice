Notes:

1. Install Tensorflow of a particular version: 

pip install tensorflow==1.5.0 

(It uninstalls currently installed tensorflow and then installs the specified version 1.5.0)


2. Install matplotlib

pip install matplotlib

3. Error: UnicodeDecodeError: 'gbk' codec can't decode byte 0xa6 in position 1362: illegal multibyte sequence
Fix: Add encoding parameter to ext = open(filename, 'r', encoding='utf-8').readlines()[1:] in method read_birth_life_data


3. os.environ['TF_CPP_MIN_LOG_LEVEL'] = '0', '1', '2'
Defaults to 0, so all logs are shown. 
Set TF_CPP_MIN_LOG_LEVEL to 1 to filter out INFO logs, 
2 to additionall filter out WARNING, 3 to additionally filter out ERROR.
